# Citações

> Eu sou uma citação\
> que continua na linha\
> seguinte

> Eu sou uma citação
>
> que continua na linha
>
> seguinte

> **ATENÇÃO**
>
> Não faça isso!
>
> Pode danificar o sistema!

# Texto sobrescrito, subscrito e sublinhado

Exemplo de texto <sup>sobrescrito</sup>, outro <sub>subscrito</sub> e um último <ins>sublinhado</ins>\

O cálculo da hipotenusa é: hipotenusa<sup>2</sup>=x<sup>2</sup>+y<sup>2</sup>

# Blocos de código


```C#
[C# Code]

string teste = string.Empty;
int x = 5;

if(x == int.Parse(teste))
{
	//TODO: Desenvolva aqui. 
}
```

~~~SQL
[SQL Code]

Insert into TB_Veiculo Values(
IdVeiculo = 1,
Placa = XXX0Y23
)
~~~

## Código em linha

asdfasd asdfadsfasdf `pip install flet` asdfasdf\
`git clone` https://asdçlkjasdfçlkjasdf\

Para salvar as alterações da versão usamos:
- `git add`: Adiciona os arquivos para commit
- `git commit -m ""`: Para escrever a mensagem de commit
- `git push`: Para enviar o commit para o servidor.
